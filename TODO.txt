* Manage rendering with a central object that handles RAF callbacks
* Stop rendering if off screen
* Parallax support via data-parallax=[buffer-amount-in-px]
* Background breakpoint support
* Better srcset support
* Detect and throttle if dropping frames
* Ability to animate points on beziers
* Animated gif background support

see also $ grep TODO -r .
